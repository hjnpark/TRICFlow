name: tricflow
channels:
  - conda-forge
  - psi4/label/dev
dependencies:
  - python>=3.10
  - numpy
  - scipy
  - networkx
  - matplotlib
  - pip
  - pip:
    # TRICS interpolation requires the geomeTRIC interpolate branch:
    #   pip install -e ../geomeTRIC
    - -e ../geomeTRIC